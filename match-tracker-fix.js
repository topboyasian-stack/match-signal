/* Match Signal Match Tracker reliability + live-state correction layer — PAPER ONLY. */
(function(){'use strict';
const qs=new URLSearchParams(location.search),eventId=qs.get('event_id');if(!eventId||!location.pathname.toLowerCase().includes('match-tracker'))return;
const esc=v=>{const d=document.createElement('div');d.textContent=String(v??'');return d.innerHTML};const num=v=>{const n=Number(v);return Number.isFinite(n)?n:null};const clamp=(x,a,b)=>Math.max(a,Math.min(b,x));
const leagues={EPL:'eng.1','La Liga':'esp.1',Bundesliga:'ger.1',Serie A:'ita.1',Ligue 1:'fra.1','Champions League':'uefa.champions',MLS:'usa.1','Primeira Liga':'por.1'};let lastPrediction=null,lastSummary=null,lastAt=0,working=false;
async function get(u){const r=await fetch(u,{cache:'no-store'});if(!r.ok)throw Error('HTTP '+r.status);return r.json()}
async function prediction(){try{const d=await get('./data/predictions.json?v='+Date.now());return(Array.isArray(d)?d:[]).find(x=>String(x.event_id)===String(eventId))||null}catch(_){return null}}
function sport(p){return p?.sport||'football'}
async function scoreboard(p){const s=sport(p);if(s==='football'){const slug=leagues[p?.league]||'usa.1';const d=await get('https://site.api.espn.com/apis/site/v2/sports/soccer/'+slug+'/scoreboard');return(d.events||[]).find(x=>String(x.id)===String(eventId))||null}if(s==='basketball'){const slug=String(p?.league||'nba').toLowerCase().replace(/\s+/g,'.');const d=await get('https://site.api.espn.com/apis/site/v2/sports/basketball/'+slug+'/scoreboard');return(d.events||[]).find(x=>String(x.id)===String(eventId))||null}for(const tour of ['atp','wta']){try{const d=await get('https://site.api.espn.com/apis/site/v2/sports/tennis/'+tour+'/scoreboard');for(const g of d.events||[])for(const z of g.groupings||[])for(const c of z.competitions||[])if(String(c.id)===String(eventId))return{id:eventId,name:c.name,status:c.status,competitions:[c],notes:c.notes,season:c.season}}catch(_){}}return null}
async function enrich(e,p){if(!e)return e;let summary=null,s=sport(p);if(s==='football'){const slug=leagues[p?.league]||'usa.1';try{summary=await get('https://site.api.espn.com/apis/site/v2/sports/soccer/'+slug+'/summary?event='+encodeURIComponent(eventId))}catch(_){try{const cid=e?.competitions?.[0]?.id||eventId;summary=await get('https://sports.core.api.espn.com/v2/sports/soccer/leagues/'+slug+'/events/'+eventId+'/competitions/'+cid+'/plays?limit=500')}catch(_){}}}else if(s==='basketball'){const slug=String(p?.league||'nba').toLowerCase().replace(/\s+/g,'.');try{summary=await get('https://site.api.espn.com/apis/site/v2/sports/basketball/'+slug+'/summary?event='+encodeURIComponent(eventId))}catch(_){}}else{for(const tour of ['atp','wta']){try{summary=await get('https://site.api.espn.com/apis/site/v2/sports/tennis/'+tour+'/summary?event='+encodeURIComponent(eventId));if(summary)break}catch(_){}}}lastSummary=summary||null;if(!summary)return e;const out=JSON.parse(JSON.stringify(e));if(summary.header?.competitions?.[0]){out.competitions=out.competitions||[{}];out.competitions[0]={...out.competitions[0],...summary.header.competitions[0]}}if(Array.isArray(summary.plays)){out.plays=summary.plays;out.details=summary.plays;out.competitions=out.competitions||[{}];out.competitions[0].details=summary.plays}else if(Array.isArray(summary.items)){out.plays=summary.items;out.details=summary.items;out.competitions=out.competitions||[{}];out.competitions[0].details=summary.items}if(summary.boxscore)out.boxscore=summary.boxscore;if(summary.commentary)out.commentary=summary.commentary;return out}
function state(e){return String(e?.status?.type?.description||e?.status?.type?.shortDetail||e?.status?.type?.name||'').toLowerCase()}
function live(e){const s=state(e);return e?.status?.type?.state==='in'||/in progress|live|first half|second half|quarter|set/.test(s)}
function score(e){const cs=e?.competitions?.[0]?.competitors||[];return{home:num(cs.find(x=>x.homeAway==='home')?.score)??0,away:num(cs.find(x=>x.homeAway==='away')?.score)??0}}
function minute(e){const raw=e?.status?.displayClock??e?.status?.clock,m=String(raw??'').match(/(\d+)(?::(\d{2}))?/);return m?Number(m[1])+Number(m[2]||0)/60:null}
function footballLiveProb(p,e){const base=p?.probabilities||{};let h=clamp(num(base.p1)??.5,.01,.98),d=clamp(num(base.draw)??.25,.01,.98),a=clamp(num(base.p2)??.25,.01,.98);const sc=score(e),m=sc.home-sc.away,min=minute(e)??45,late=clamp((min-30)/65,0,1),margin=Math.min(2,Math.abs(m));if(m){const leadBoost=1+(.65+.55*late)*margin,trailFactor=1-(.32+.20*late)*margin,drawFactor=1-(.18+.12*late)*margin;if(m>0){h*=leadBoost;a*=clamp(trailFactor,.25,1);d*=clamp(drawFactor,.35,1)}else{a*=leadBoost;h*=clamp(trailFactor,.25,1);d*=clamp(drawFactor,.35,1)}}let sum=h+d+a;let out={home:h/sum,draw:d/sum,away:a/sum};if(m<0){const cap=clamp(.64-.18*late,.45,.64);if(out.home>cap){const excess=out.home-cap;out.home=cap;out.away+=excess*.72;out.draw+=excess*.28}}else if(m>0){const cap=clamp(.64-.18*late,.45,.64);if(out.away>cap){const excess=out.away-cap;out.away=cap;out.home+=excess*.72;out.draw+=excess*.28}}sum=out.home+out.draw+out.away;return{home:out.home/sum,draw:out.draw/sum,away:out.away/sum}}
function installProbabilityOverride(){const api=window.MatchSignalLiveAnalysis;if(!api?.football||api.football.__stateCorrected)return;const original=api.football;const wrapped=function(p,e){const a=original(p,e)||{};a.live_probabilities=footballLiveProb(p,e);a.method=(a.method||'')+' + heuristic score-state correction';return a};wrapped.__stateCorrected=true;api.football=wrapped}
function stats(e){const rows=[];for(const c of e?.competitions?.[0]?.competitors||[])for(const x of c.statistics||[])rows.push({side:c.homeAway,name:x.displayName||x.name||x.abbreviation,value:x.displayValue??x.value});if(e?.boxscore?.teams)for(const t of e.boxscore.teams)for(const x of t.statistics||[])rows.push({side:t.homeAway||t.team?.homeAway,name:x.displayName||x.name||x.abbreviation,value:x.displayValue??x.value});return rows}
function timeline(e){const src=Array.isArray(e?.plays)?e.plays:Array.isArray(e?.details)?e.details:[];return src.map(x=>({time:x.clock?.displayValue||x.clock||x.period?.displayValue||x.period||x.type?.shortText||'',text:x.text||x.shortText||x.description||x.type?.text||x.type?.description||''})).filter(x=>x.text).slice(-12).reverse()}
function renderProvider(e){let box=document.getElementById('ms-provider-live');if(!box){box=document.createElement('section');box.id='ms-provider-live';box.className='panel';const app=document.getElementById('app');if(app)app.appendChild(box)}const rows=stats(e),tl=timeline(e),sc=score(e);const q=rows.length?rows.slice(0,18).map(x=>'<div class="stat"><div class="statName">'+esc(String(x.name))+'</div><div class="statPair"><b>'+esc(x.side==='home'?x.value:'—')+'</b><span>vs</span><b>'+esc(x.side==='away'?x.value:'—')+'</b></div></div>').join(''):'';const events=tl.length?tl.map(x=>'<div class="event"><div class="eventTime">'+esc(x.time||'LIVE')+'</div><div class="eventText">'+esc(x.text)+'</div></div>').join(''):'<div class="empty">No detailed play-by-play returned. Score and clock tracking remain active.</div>';box.innerHTML='<h2>Provider live feed</h2><div class="quality"><span class="tag">event '+esc(eventId)+'</span><span class="tag">score '+sc.home+'–'+sc.away+'</span><span class="tag">'+(live(e)?'LIVE':'NOT LIVE')+'</span><span class="tag">refresh '+Math.round((Date.now()-lastAt)/1000)+'s ago</span></div><div class="notice" style="margin:10px 0">'+(lastSummary?.header?'Summary endpoint enriched the tracker with provider state, plays and statistics.':lastSummary?'Core provider play feed enriched the tracker.':'Scoreboard fallback is active; detailed provider enrichment is currently unavailable.')+'</div><div class="stats">'+(q||'<div class="empty">No detailed team statistics were supplied by the provider. No synthetic pressure score is shown.</div>')+'</div><h2 style="margin-top:16px">Recent events</h2><div class="timeline">'+events+'</div>'}
function refreshModel(p,e){installProbabilityOverride();const api=window.MatchSignalLiveAnalysis;if(!api)return;const s=sport(p),a=api[s]?.(p,e);if(!a)return;const probs=a.live_probabilities,vals=s==='football'?[['Home',probs.home],['Draw',probs.draw],['Away',probs.away]]:[['Home',probs.home],['Away',probs.away]];document.querySelectorAll('.prob').forEach((card,i)=>{if(!vals[i])return;const [name,v]=vals[i],b=card.querySelector('.probHead b'),fill=card.querySelector('.fill'),lab=card.querySelector('.probHead span');if(b)b.textContent=Math.round(v*100)+'%';if(fill)fill.style.width=Math.round(v*100)+'%';if(lab)lab.textContent=name});let ou=document.getElementById('ms-corrected-ou');if(!ou){ou=document.createElement('section');ou.id='ms-corrected-ou';ou.className='panel';const anchor=document.getElementById('ms-live-trace');if(anchor)anchor.before(ou);else document.getElementById('app')?.appendChild(ou)}if(ou&&a.totals)ou.innerHTML='<h2>Live expected O/U · corrected state model</h2><div class="heroGrid"><div class="heroMetric"><small>Projected final</small><b>'+(a.totals.expected==null?'—':Number(a.totals.expected).toFixed(2))+'</b></div><div class="heroMetric"><small>Line</small><b>'+(a.totals.line==null?'—':Number(a.totals.line).toFixed(1))+'</b></div><div class="heroMetric"><small>Lean</small><b>'+esc(a.totals.lean||'Unavailable')+'</b></div><div class="heroMetric"><small>Driver</small><b>'+esc(a.totals.driver||'Live state')+'</b></div></div><p class="notice">'+esc(a.totals.source||'Dynamic live-state projection')+'. PAPER ONLY — UNVALIDATED.</p>';}
async function run(){if(working)return;working=true;try{const p=lastPrediction||await prediction();lastPrediction=p;if(!p){working=false;return}let e=await scoreboard(p);if(!e){working=false;return}e=await enrich(e,p);lastAt=Date.now();refreshModel(p,e);const fake=document.querySelector('.msCentre');if(fake&&!stats(e).length)fake.remove();renderProvider(e);const status=document.getElementById('statusText'),updated=document.getElementById('updated');if(status)status.textContent=live(e)?'LIVE · provider synchronized':'Match state synchronized';if(updated)updated.textContent='Live provider refresh '+new Date().toLocaleTimeString()+' · tracker auto-refresh'}catch(err){console.debug('tracker fix',err)}finally{working=false}}
window.MatchSignalTrackerFix={refresh:run};setTimeout(run,1200);setInterval(run,10000);
})();